{
  "name": "lanball",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @lanball/client",
    "test": "vitest run",
    "typecheck": "tsc -p packages/sim && tsc -p packages/client"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^4.1.11"
  }
}
